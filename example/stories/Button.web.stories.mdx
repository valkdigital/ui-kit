import {
  Meta,
  Story,
  Preview,
  Props,
  ColorPalette,
  ColorItem,
} from "@storybook/addon-docs/blocks";
import { View } from "react-native";
import {
  Button,
  Text,
  Fab,
  MultipleButtons,
  Stepper,
} from "@valkdigital/ui-kit";
import { WithFigma } from "storybook-addon-figma";
import Buttons from "../storybook/stories/Buttons";
import AllFAB from "../storybook/stories/FAB";
import { withKnobs, boolean, color } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";

<Meta title="Buttons" />

# CTA

<Preview>
  <Story
    name="CTA"
    decorators={[withKnobs]}
    parameters={{
      design: {
        type: "figma",
        url:
          "https://www.figma.com/file/7Sh8nuwN3NqpwMCoOUPu9G/Styleguide-Test?node-id=104%3A525",
      },
    }}
  >
    <Buttons
      loading={boolean("loading", false, "cta")}
      disabled={boolean("disabled", false, "cta")}
      color={color("color", undefined, "cta")}
      labelColor={color("label color", undefined, "cta")}
    />
  </Story>
</Preview>

## Props

<Props of={Button} />

# FAB

<Preview>
  <Story
    name="FAB"
    decorators={[withKnobs]}
    parameters={{
      design: {
        type: "figma",
        url:
          "https://www.figma.com/file/7Sh8nuwN3NqpwMCoOUPu9G/Styleguide-Test?node-id=104%3A525",
      },
    }}
  >
    <AllFAB
      color={color("color", "#1AA0E2", "fab")}
      backgroundColor={color("background color", "#2BB9F5", "fab")}
      iconColor={color("icon color", "#FFFFFF", "fab")}
    />
  </Story>
</Preview>

## Props

<Props of={Fab} />

# Multiple Buttons

<Preview>
  <Story
    name="Multiple Buttons"
    decorators={[withKnobs]}
    parameters={{
      design: {
        type: "figma",
        url:
          "https://www.figma.com/file/7Sh8nuwN3NqpwMCoOUPu9G/Styleguide-Test?node-id=104%3A525",
      },
    }}
  >
    <View style={{ marginTop: 16 }}>
      <MultipleButtons
        activeColor={color("active color", "#2BB9F5", "multiple buttons")}
        inActiveColor={color("inactive color", "#E3E3E3", "multiple buttons")}
        onPress={(i) => console.log(i)}
        labels={["btn 1", "button", "button 12"]}
      />
    </View>
  </Story>
</Preview>

## Props

<Props of={MultipleButtons} />
