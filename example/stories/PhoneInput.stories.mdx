import {
  Meta,
  Story,
  Preview,
  Props,
  ColorPalette,
  ColorItem,
} from "@storybook/addon-docs/blocks";
import { View } from "react-native";
import { PhoneInput, countries, Spacing } from "@valkdigital/ui-kit";

<Meta title="PhoneInput" />

# Preview

<Preview>
  <Story
    name="PhoneInput"
    parameters={{
      design: {
        type: "figma",
        url:
          "https://www.figma.com/file/JdbljcIvyMoEVsBZgsg7bbu8/1.-Apps-Styleguide-and-Components?node-id=2451%3A0",
      },
    }}
  >
    <View
      style={{
        justifyContent: "space-evenly",
        alignItems: "center",
        flex: 1,
        paddingHorizontal: Spacing.sp2,
      }}
    >
      <PhoneInput
        label="Phone"
        containerStyle={{ marginBottom: Spacing.sp2 }}
        countries={countries.en}
        favoriteCountries={["NL", "BE", "DE", "GB"]}
        defaultCountry="NL"
        listTitle="Phone"
        listEmptyText="No results found"
        listSearchPlaceholder="Search"
        onChangeText={(text) => console.log("onChange", text)}
      />
    </View>
  </Story>
</Preview>

# Props

<Props of={PhoneInput} />
