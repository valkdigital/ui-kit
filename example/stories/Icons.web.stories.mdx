import { Meta, Preview, Story, Props } from "@storybook/addon-docs/blocks";
import {
  ThemeManager,
  Text,
  Icon,
  Spacing,
  IconSet,
} from "@valkdigital/ui-kit";

<Meta title="Icons" />

# Icons

<Preview>
  <Story
    name="Icons"
    parameters={{
      design: {
        type: "figma",
        url:
          "https://www.figma.com/file/KVbqwH1I4lStgUwTK4Wr8z/App-Design-System?node-id=1304%3A6046",
      },
    }}
  >
    <View
      style={{
        flexDirection: "column",
        flexWrap: "wrap",
        overflow: "hidden",
        paddingHorizontal: Spacing.sp1,
        paddingTop: Spacing.sp1,
      }}
    >
      <Text type="h2">Icons default (outline)</Text>
      <View
        style={{
          alignItems: "flex-start",
          display: "flex",
          flexDirection: "row",
          flexWrap: "wrap",
          maxWidth: 1400,
        }}
      >
        {IconSet.names.map((name) => (
          <View
            style={{
              borderWidth: 1,
              borderColor: "black",
              borderStyle: "solid",
              display: "flex",
              margin: Spacing.sp1,
              flexDirection: "column",
              padding: Spacing.sp1,
            }}
            key={`${name}-wrapper`}
          >
            <Text type="h4">{name}</Text>
            <Icon name={name} size="small" />
            <Icon name={name} size="medium" />
            <Icon name={name} size="large" />
            <Icon name={name} size="xlarge" />
          </View>
        ))}
      </View>
      <Text type="h2">Icons solid</Text>
      <View
        style={{
          alignItems: "flex-start",
          display: "flex",
          flexDirection: "row",
          flexWrap: "wrap",
          maxWidth: 1400,
        }}
      >
        {IconSet.names.map((name) => (
          <View
            style={{
              borderWidth: 1,
              borderColor: "black",
              borderStyle: "solid",
              display: "flex",
              margin: Spacing.sp1,
              flexDirection: "column",
              padding: Spacing.sp1,
            }}
            key={`${name}-wrapper`}
          >
            <Text type="h4">{name}</Text>
            <Icon name={name} size="small" solid={true} />
            <Icon name={name} size="medium" solid={true} />
            <Icon name={name} size="large" solid={true} />
            <Icon name={name} size="xlarge" solid={true} />
          </View>
        ))}
      </View>
    </View>
  </Story>
</Preview>

## Props

<Props of={Icon} />
